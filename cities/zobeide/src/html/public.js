import { createRoot } from 'react-dom/client';
import { Zobeide } from '../pages/Zobeide';

const domNode = document.getElementById('root');
const root = createRoot(domNode);

root.render(<Zobeide />);
