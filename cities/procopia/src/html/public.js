import { createRoot } from 'react-dom/client';
import { TheIndagoInn } from '../pages/TheIndagoInn';

const domNode = document.getElementById('root');
const root = createRoot(domNode);

root.render(<TheIndagoInn />);
